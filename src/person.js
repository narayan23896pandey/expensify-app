/*const isAdults = (age) => (age >= 18)
const isDrinkable = (age) => (age >= 21)

export { isAdults, isDrinkable };*/
console.log("person is running!");
export const isAdults = (age) => (age >= 18);
export const isDrinkable = (age) => (age >= 21);
const isSenior = (age) => (age >= 65);
export default isSenior;